# 03 - Random Smoke Summary

Date: 2026-05-26

## Status

The random smoke harness is present, but runtime is intentionally blocked until
the Phase 3 full `CV32E40P + HPDCache` simulation can advance reliably.

The current blocker is not a random-test compile problem. The same full-design
runtime path used by the random TB times out on ModelSim Intel FPGA Starter
before useful simulation progress is observed.

## Files created

- `rtl/l1_cache/work/tb/tb_cv32e40p_dcache_random.sv`
- `rtl/l1_cache/work/sim/cv32e40p_dcache_random.f`
- `rtl/l1_cache/work/sim/run_cv32_dcache_random.ps1`
- `rtl/l1_cache/work/sim/Makefile`

## Commands run

Compile syntax check for the random TB:

```powershell
powershell -ExecutionPolicy Bypass -Command "vlog -sv -work msim_cv32_dcache -f cv32e40p_dcache_random.f"
```

Result:

```text
PASS, Errors: 0
```

The seed runtime scripts have not been used for final measurement yet because
they would hit the same Phase 3 full-design timeout.

## Random test contents

The random TB generates a small RISC-V program before reset is released:

- random load word
- random store word
- cacheable RAM addresses
- same-index/different-tag addresses to encourage eviction
- DONE store to `0x2000_0004`

## Seed status

| Seed | Compile | Runtime | Timeout | X/Z check | Deadlock valid/ready | Load/store count | Hit/miss count | VBUF/write-back count |
|---|---|---|---|---|---|---|---|---|
| 1 | Not run in seed script | Blocked by Phase 3 runtime | Expected until full sim advances | Not measured | Not measured | Generated by TB | Not measured | Not measured |
| 2 | Not run in seed script | Blocked by Phase 3 runtime | Expected until full sim advances | Not measured | Not measured | Generated by TB | Not measured | Not measured |
| 3 | Not run in seed script | Blocked by Phase 3 runtime | Expected until full sim advances | Not measured | Not measured | Generated by TB | Not measured | Not measured |

Expected log paths when the run is enabled:

- `rtl/l1_cache/logs/04_random_seed_1.log`
- `rtl/l1_cache/logs/04_random_seed_2.log`
- `rtl/l1_cache/logs/04_random_seed_3.log`

## Next step

After the Phase 3 full simulation advances beyond reset, run:

```powershell
cd rtl/l1_cache/work/sim
powershell -ExecutionPolicy Bypass -File .\run_cv32_dcache_random.ps1
```

Then update this table with:

- pass/fail for each seed,
- timeout status,
- X/Z status,
- valid/ready deadlock status,
- load/store count,
- hit/miss count if exposed,
- write-back/VBUF alloc count if exposed.
